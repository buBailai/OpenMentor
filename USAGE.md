# OpenMentor 使用手册

> 老师 5 分钟搭出第一个 AI 导师的完整指南。

## 一、登录

浏览器打开 `http://你的IP:5001`，使用 `admin / openmentor` 登录（首次请修改密码）。

> 没安装好？看 [INSTALL.md](./INSTALL.md)。

## 二、配置 AI 模型

第一次使用必须先配置一个国产大模型的 API Key。

1. 顶栏右上角你的用户名 → **个人设置**
2. 在 「AI 配置」标签页选择一家模型（推荐 **DeepSeek** —— 申请最快、对话快）
3. 把 API Key 粘贴到对应的输入框
4. 点「测试 API 密钥」验证有效性
5. 点「保存配置」

各模型 API Key 申请地址见 [INSTALL.md](./INSTALL.md#五配置-ai必做)。

## 三、创建你的第一个 AI 导师

### Step 1 进入 AI 导师管理页

顶栏导航 → **AI 导师**（机器人图标 🤖）

### Step 2 点击「创建 AI 导师」

填写以下字段：

| 字段 | 是否必填 | 示例 |
|---|---|---|
| 助学名称 | 必填 | `高中数学解题导师` |
| 简介 | 选填 | `面向高一学生的数学解题引导助手` |
| 选择模型 | 必填 | DeepSeek（默认） |
| **系统提示词** | 必填 | 看下方 |
| 学生欢迎语 | 选填 | `你好！我是数学导师，遇到不会的题随时问～` |
| 多模态开关 | 选填 | 图片/文件输入默认开 |
| 每日消息上限 | 默认 50 | |
| 链接有效期 | 默认 48h | |
| 关键词黑名单 | 默认 4 类 | |

### Step 3 系统提示词怎么写？

不会写没关系！点击右上角紫色按钮 **🤖 AI 帮我写提示词**。

在弹窗里回答 5 个问题：

1. **角色定位**：例 `高中数学解题导师`
2. **目标学生**：例 `高一基础薄弱学生`
3. **期望行为**：例 `先反问关键概念再讲解，不直接给答案`
4. **禁止行为**：例 `不抄袭、不评论同学`
5. **语气风格**：例 `耐心、用比喻`

可选：上传一份相关教材 PDF/Word，AI 会把要点融入提示词。

点「🪄 生成草稿」 → 等约 5-30 秒 → 看到 800 字的专业提示词 → 点「✓ 采用到主表单」。

### Step 4 保存创建

点「创建 AI 导师」按钮，跳到详情页。

## 四、把链接分享给学生

详情页中央有：

- **学生访问链接**：复制按钮一键复制
- **二维码**：右侧自动生成，可投屏或截图发到班级群

> ⚠️ 学生设备必须与教师机在**同一局域网/校园网**。

学生使用流程：
1. 扫码 / 点链接
2. 填写「班级」+「姓名」
3. 进入聊天界面，开始提问

## 五、实时审计学生对话

详情页 → 点 **「对话审计」** 按钮（蓝色，眼睛图标 👁）

审计大屏功能：

- 📊 **统计卡片**：已对话学生数 / 消息总数 / 黑名单触发次数 / 助学状态
- 📋 **学生会话列表**：每行展示一名学生的班级、姓名、消息数、今日额度、最近活动
- ⚠️ **异常高亮**：触发黑名单的会话有黄色徽章，已封禁的有红色徽章
- 🔄 **自动刷新**：每 8 秒自动刷新数据

点某个会话的「查看对话」 → 进入该学生的完整消息流，每 5 秒自动更新。

需要"踢出"某个学生 → 「封禁该学生」按钮，对方立即无法继续对话。

## 六、导出 / 分享 / 克隆

### 导出对话为 Excel

审计大屏 → 「导出全部对话 Excel」 → 下载 `.xlsx` 文件，可作为教研数据保存。

### 导出 AI 导师模板（与同事分享）

详情页 → 「导出模板」 → 下载 `.json` 文件 → 发给同事。

> 模板**仅包含配置**（系统提示词、欢迎语、多模态开关等），**不包含**：
> - API Key（同事需自己填）
> - 对话历史
> - 上传的附件

### 克隆已有助学

详情页 → 「克隆」 → 自动创建一份副本，可在副本基础上修改。

### 导入同事分享的模板

AI 导师列表 → 「导入模板」 → 上传 JSON 文件或粘贴 JSON 文本 → 确认。

## 七、紧急熔断

发现学生在滥用、出 bug 或 API 烧太快？

详情页 → **「禁用（紧急熔断）」** 按钮 → 立即禁用，所有学生显示「该 AI 导师已被老师暂时关闭」。

修好后再点同一按钮启用。

## 八、安全策略调优

### 调整每日消息上限

编辑 AI 导师 → 「每生每日消息上限」 → 修改后保存。

> 默认 50 条。考试季可以调到 30 条防刷题，自由探索可以调到 100 条。

### 自定义关键词黑名单

编辑 AI 导师 → 「关键词黑名单」 → 4 类分组开关 + 自定义关键词。

四类默认黑名单：
- 暴力自残（默认开）
- 色情低俗（默认开）
- 越狱提示词（默认开）—— 防学生绕过设定
- 学习作弊（默认关）—— 比如「直接给我答案」

### 链接续期

编辑 AI 导师 → 勾选「重置有效期」 → 选时长（24h/48h/72h/168h）→ 保存。

## 九、和 QuickForm 一起用

OpenMentor 保留了 QuickForm 全部原有功能（数据任务、HTML 表单收集、AI 报告等）。

典型组合用法：
- 用 **AI 导师** 让学生与 AI 对话学习
- 用 **数据任务** 收集学生的 HTML 互动作品

详见顶栏导航的「数据任务」「导入任务」菜单。

## 十、常见问题

### 我的 API Key 会被泄露吗？

不会。API Key 仅存在你本地的 SQLite 数据库。OpenMentor 不上传任何数据到外部服务器（除非调用 LLM API 时本次消息）。

### 学生能看到其他学生的对话吗？

不能。每个学生的对话完全独立隔离。只有老师在审计大屏能看到所有学生。

### 如果一个学生发了不当内容怎么办？

3 个保护层：
1. **关键词黑名单**自动拦截 + 记录命中事件
2. **审计大屏**老师可看到完整对话
3. **封禁该学生**立即停止其使用

### 学生作弊怎么办（让 AI 直接做作业）？

- 系统提示词中明确写「先引导思考，不直接给答案」
- 启用「学习作弊」黑名单类别
- 审计大屏定期查看，发现作弊行为及时引导

### 一个老师能开多少个 AI 导师？

无上限。每个助学独立运作，互不影响。

### 数据怎么备份？

整个项目目录复制一份就行。核心数据在 `openmentor.db`（SQLite）+ `static/uploads/`（附件）。

---

🎉 现在你已掌握全部功能！开始为你的学生设计第一个 AI 导师吧。
